<% @sections.each do |section| %>
	<div class="section-content tab-pane <%= 'active' if section == @sections.find_by_name!(@current_tab) %>" id="<%= section.name %>" role="tabpanel">		
		<%= render 'home/section', food_items: section.food_items %>
	</div>

<% end %>